# Copy this file to .env and fill in the values
# Your Gemini / Google Generative Language API key (or other model key)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
# Optional: override the default Generative Language endpoint
# By default the backend uses Google's text-bison endpoint. You can change this to another provider if needed.
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta2/models/text-bison-001:generate
# Or set to another provider endpoint if you use a different model/provider.
# Server port
PORT=5000
